
  <% if flash[:notice] %>
      <div class= "flash">
        <%= flash[:notice] %>
      </div>
  <% end %>

  <p>Title
  	<%= @booker.title %>
  </p>
  <p>Body
  	<%= @booker.body %>
  </p>

  <%= link_to "edit", edit_booker_path(@booker.id) %>
  <%= link_to "back", new_booker_path(@booker.id) %>