
  <% if flash[:notice] %>
      <div class= "flash">
        <%= flash[:notice] %>
      </div>
  <% end %>

  <p>Title
  	<%= @booker.title %>
  </p>
  <p>Body
  	<%= @booker.body %>
  </p>

  <%= link_to "Edit", edit_booker_path(@booker.id), class: "edit_#{@booker.id}" %>
  <%= link_to "Back", new_booker_path(@booker.id), class: "back" %>